<header class="page-header">
  <h3>New Comment</h3>
</header>

<%= bootstrap_form_with model: [ticket, comment] do |f| %>
  <%= f.text_area :text %>
  <%= f.select :state_id, @states.map{|state| [state.name, state.id]} %>
  <br>
  <%= f.primary %>
<% end %>
