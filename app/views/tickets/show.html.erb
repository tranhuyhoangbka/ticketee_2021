<div class="ticket">
  <header>
    <h1><%= @project.name %></h1>
  </header>
  <header>
    <h2><%= @ticket.name %></h2>
  </header>

  <ul class="actions">
    <li><%= link_to 'Edit Ticket', [:edit, @project, @ticket] %></li>
  </ul>

  <%= simple_format(@ticket.description) %>
</div>
